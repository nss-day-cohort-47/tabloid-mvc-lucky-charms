@model TabloidMVC.Models.ViewModels.TagManagerViewModel

@{
    ViewData["Title"] = "TagListByPost";
}

<h1>Tags</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AddedTags)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AddedTags) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            @*<td>
                @Html.ActionLink("Edit", "Edit", new { id=tag.Id }) |
                @Html.ActionLink("Details", "Details", new { id=tag.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=tag.Id })
            </td>*@
        </tr>
}
    </tbody>
</table>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NotAddedTags)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.NotAddedTags)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                @*<td>
                        @Html.ActionLink("Edit", "Edit", new { id=tag.Id }) |
                        @Html.ActionLink("Details", "Details", new { id=tag.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id=tag.Id })
                    </td>*@
            </tr>
        }
    </tbody>
</table>
